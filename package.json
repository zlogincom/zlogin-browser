{
	"name": "zlogin-public",
	"version": "0.1.0",
	"private": true,
	"description": "Public ZLogin CLI, MCP, core contracts, and agent skills monorepo scaffold",
	"packageManager": "pnpm@10.14.0",
	"license": "MIT",
	"scripts": {
		"build": "pnpm --filter @zlogin/core build && pnpm --filter zlogin-mcp build",
		"check": "node scripts/check-structure.mjs && pnpm --filter @zlogin/core check && pnpm --filter zlogin-mcp check",
		"test": "pnpm --filter zlogin-mcp test",
		"format": "prettier --write .",
		"format:check": "prettier --check ."
	},
	"devDependencies": {
		"@types/node": "^24.3.0",
		"prettier": "^3.6.2",
		"typescript": "^5.9.2"
	}
}

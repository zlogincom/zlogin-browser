# zlogin-core

Shared contracts and capability metadata for the public ZLogin integrations.

The package owns stable server identity, agent guidance, profile selector types, response metadata, rate-limit contracts, and the public Runtime release manifest used by `@zlogin/cli` and `zlogin-mcp`. It intentionally has no dependency on a terminal UI or a specific AI client.

# @zlogin/mcp

The planned Model Context Protocol adapter for ZLogin.

The MCP package will expose validated tools, structured results, and explicit read-only or destructive annotations. It will use `@zlogin/core` for public contracts and remain a local stdio integration unless a separately reviewed remote transport is approved.
